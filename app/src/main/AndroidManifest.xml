<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fdf.ajs.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Navigation"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".planer.BrugerOvelseActivity"
            android:exported="false" />
        <activity
            android:name=".planer.BrugerPlanActivity"
            android:exported="false" />
        <activity android:name=".planer.PlanActivity" />
        <activity android:name=".planer.EditOvelser" />
        <activity android:name=".planer.PlanerActivity" />
        <activity android:name=".user.HomeActivity" />
        <activity android:name=".planer.OvelseInfoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>